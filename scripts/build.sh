#!/bin/bash
set -e
# Build ChromePilot extension
# No build step needed — plain files loaded directly by Chrome
# If Vite is added later, replace with: npm run build

echo "ChromePilot is a plain JS extension. No build step required."
echo "Load the /src directory directly in chrome://extensions (Developer Mode)."
